{
  "default": {
    "paths": ["../../features/specification/*.feature", "../../features/planning/*.feature"],
    "import": ["test/steps/**/*.ts"],
    "tags": "not @backlog",
    "format": ["progress"]
  }
}
