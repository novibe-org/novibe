{
  "default": {
    "paths": [
      "../../features/specification/*.feature",
      "../../features/planning/*.feature",
      "../../features/status/*.feature"
    ],
    "import": ["test/steps/**/*.ts"],
    "tags": "not @backlog",
    "format": ["progress", "message:test/results/messages.ndjson"]
  }
}
