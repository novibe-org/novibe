import { defineConfig } from "likec4/config";

export default defineConfig({
  name: "novibe",
  metadata: {
    description: `NoVibe's architecture after arc42: context; building blocks down to the portal's parts; a runtime
scenario per flow; deployment. The why is in docs/adrs, the what in features/.`,
  },
});
